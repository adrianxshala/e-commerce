<%- include("./partials/head") %>
</head>
<body>
   <%- include("./partials/navigation") %>
    <main>
        <h1 class="<%= true ? "red" : "black" %>">Home is Coming soon</h1>
        <% if(10 > 9) { %>
            <h1>Hello from EJS </h1>
        <% } %>
        <% for(let i = 0; i < 10; i++) { %>
            <p> <%= i %> </p>
        <% } %>

        <div> <%= personInfo.firstName %> </div>
        <div> <%= personInfo.lastName %> </div>
        <div> <%= personInfo.job %> </div>
    </main>
</body>
</html>